---
import { ArrowLeft } from "@lucide/astro";
import RealTimeSavings from "../../components/RealTimeSavings/RealTimeSavings";
import "../../styles/global.css";
---

<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta content="width=device-width, initial-scale=1" name="viewport" />
    <title>Savings From Budget Cuts</title>
  </head>
  <body class="bg-gray-50 text-gray-800">
    <main
      class="container mx-auto px-4 py-16 flex flex-col items-center text-center"
    >
      <a
        aria-label="Back to home"
        class="self-start mb-6 text-gray-600 transition-colors duration-300 hover:text-gray-900"
        href="/"
      >
        <ArrowLeft class="h-6 w-6 sm:h-7 sm:w-7" />
      </a>

      <h1
        class="mt-6 text-3xl font-extrabold tracking-tight text-gray-900 sm:text-4xl md:text-5xl"
      >
        Savings From Budget Cuts
      </h1>
      <p class="mt-2 max-w-md text-base text-gray-600 sm:text-lg md:text-xl">
        See how small cuts in your budget can grow when you invest the savings
        over time.
      </p>

      <section class="mt-10 flex w-full max-w-3xl flex-col items-center gap-8">
        <RealTimeSavings client:load />
      </section>
    </main>
  </body>
</html>
